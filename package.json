{
  "name": "sanctus-proxy",
  "version": "1.0.0",
  "description": "CORS proxy for the Sanctus Catholic mass finder — bridges frontend to MassTimes API",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.18.2"
  },
  "devDependencies": {
    "nodemon": "^3.0.2"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
